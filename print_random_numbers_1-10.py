
#imporitng a module used for generating ramndom numbers
import random
#variable declaration which stores the random generated numbers with a certain range.
my_randoms = random.sample(range(0, 10), 10)
#printing the output
print(my_randoms)
